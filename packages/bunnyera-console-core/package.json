{
  "name": "bunnyera-console-core",
  "version": "1.0.0",
  "description": "Core runtime layer for BunnyEra Console: system info, logs, config, plugins, AI runtime, filesystem, IPC handlers.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "systeminformation": "^5.23.0",
    "winston": "^3.10.0",
    "zod": "^3.22.4",
    "openai": "^4.52.0",
    "axios": "^1.7.7"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.5.4"
  }
}
